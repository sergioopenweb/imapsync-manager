"""
Blueprints — módulos de rotas organizados por domínio.
Cada módulo expõe register_routes(app) que registra as rotas diretamente no app Flask,
preservando os nomes de endpoint originais (sem prefixo de blueprint).
"""
